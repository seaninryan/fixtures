node_modules
dist
.DS_Store
.env
.env.*.local
*.local
coverage

# The snapshots live in their own repo (seaninryan/fixtures-data). A local run writes
# here so the offline rehearsal in the README keeps working; those files are scratch.
public/data/
